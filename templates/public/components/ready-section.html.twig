<section class="ready-one">
    <div class="container">
        <div class="ready-one__inner">
            {# Decorative Elements #}
            <div class="ready-one-shape-1 float-bob-x">
                <img src="{{ asset('landing/images/shapes/ready-one-shape-1.png') }}" alt="">
            </div>
            <div class="ready-one__big-icon float-bob-y-2">
                <span class="icon-fundraiser"></span>
            </div>

            {# Left Content #}
            <div class="ready-one__left">
                <div class="icon">
                    <span class="icon-fundraiser"></span>
                </div>
                <div class="content">
                    <p>{{ ready.subtitle|default('Votre histoire commence ici') }}</p>
                    <h3>{{ ready.title|default('Prêt à récolter des fonds pour votre projet ?') }}</h3>
                </div>
            </div>

            {# Right Content - CTA Button #}
            <div class="ready-one__right">
                <a href="{{ path('app.register') }}" class="thm-btn ready-one__btn">Lancez-vous</a>
            </div>
        </div>
    </div>
</section>
