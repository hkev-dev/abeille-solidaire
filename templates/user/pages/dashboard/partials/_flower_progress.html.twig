<div class="card">
    <div class="card-header border-b">
        <h3 class="card-title">Progression {{ currentFlower ? currentFlower.name : 'Violette' }}</h3>
    </div>
    <div class="card-body">
        <div class="relative pt-1">
            <div class="flex mb-2 items-center justify-between">
                <div class="text-sm font-medium text-gray-700">
                    {{ flowerProgress.received }}/{{ flowerProgress.total }} Donateurs
                </div>
                <div class="text-sm font-medium text-primary">
                    {{ flowerProgress.percentage|number_format }}%
                </div>
            </div>
            <div class="h-3 rounded-full bg-gray-100 overflow-hidden">
                <div class="h-full rounded-full bg-gradient-to-r from-primary to-primary-400 transition-all duration-500" 
                     style="width: {{ flowerProgress.percentage }}%"></div>
            </div>
            {% if flowerProgress.received == flowerProgress.total %}
                <div class="mt-2 text-sm text-success">
                    Félicitations ! Votre matrice est complète à ce niveau
                </div>
            {% else %}
                <div class="mt-2 text-sm text-gray-600">
                    Il vous manque {{ flowerProgress.remaining }} inscription{{ flowerProgress.remaining > 1 ? 's' : '' }} pour compléter ce niveau
                </div>
            {% endif %}
        </div>
    </div>
</div>
