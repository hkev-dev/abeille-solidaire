{% extends 'user/layouts/default.html.twig' %}

{% block title %}Mes anciens projets{% endblock %}

{% block content %}
    <div class="container-fixed">
    {# Stats Cards Grid #}

    <div class="grid grid-cols-1 gap-6">
        {# Main Content (Left Column) #}
        <div class="space-y-6">
            {% include 'user/pages/project/partials/_list.html.twig' %}
        </div>

        {# Sidebar Content (Right Column) #}
        <div class="space-y-6">
            {#                {% include 'user/pages/dashboard/partials/_account_status.html.twig' %} #}
            {#                {% include 'user/pages/dashboard/partials/_direct_children.html.twig' %} #}
        </div>
    </div>
    </div>{% endblock %}

{% block javascripts %}
    {{ parent() }}
{% endblock %}